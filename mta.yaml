_schema-version: "3.2"
ID: gatepassapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: gatepassapp-destination-content
  type: com.sap.application.content
  requires:
  - name: gatepassapp-destination-service
    parameters:
      content-target: true
  - name: gatepassapp-repo-host
    parameters:
      service-key:
        name: gatepassapp-repo-host-key
  - name: gatepassapp-uaa
    parameters:
      service-key:
        name: gatepassapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: gatepassapp_html_repo_host
          ServiceInstanceName: gatepassapp-html5-srv
          ServiceKeyName: gatepassapp-repo-host-key
          sap.cloud.service: gatepassapp
        - Authentication: OAuth2UserTokenExchange
          Name: gatepassapp_uaa
          ServiceInstanceName: gatepassapp-xsuaa-srv
          ServiceKeyName: gatepassapp-uaa-key
          sap.cloud.service: gatepassapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: gatepassapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: gatepassapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - gatepassapp.zip
      name: gatepassapp
      target-path: resources/
- name: gatepassapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: gatepassapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: gatepassapp-destination-service
    service-plan: lite
- name: gatepassapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: gatepassapp-xsuaa-srv
    service-plan: application
- name: gatepassapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: gatepassapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
